<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootWelcome"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:background="@android:color/white">

    <!-- Top spacing -->
    <View
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:id="@+id/topSpacer" />

    <!-- Circle with crown (using a TextView for the crown symbol) -->
    <FrameLayout
        android:id="@+id/iconFrame"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/topSpacer">

        <!-- pale circle -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/circle_bg"
            android:layout_gravity="center" />

        <!-- crown symbol (unicode) -->
        <TextView
            android:id="@+id/tvCrown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="♛"
            android:textSize="56sp"
            android:layout_gravity="center"
            android:fontFamily="sans-serif-black"
            android:textColor="#111111" />
    </FrameLayout>

    <!-- Title -->
    <TextView
        android:id="@+id/tvWelcomeTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/iconFrame"
        android:layout_marginTop="20dp"
        android:text="N-Queens Puzzle Game"
        android:textSize="28sp"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="#111111"/>

    <!-- Subtitle / short description -->
    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvWelcomeTitle"
        android:layout_marginTop="8dp"
        android:text="Place N queens so that none attack each other"
        android:textSize="16sp"
        android:gravity="center"
        android:textColor="#666666"/>

    <!-- About and Start buttons at bottom -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="28dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp">

        <Button
            android:id="@+id/btnAbout"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="ABOUT"
            android:textAllCaps="true"
            android:layout_marginBottom="12dp"/>

        <Button
            android:id="@+id/btnStart"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="START"
            android:textAllCaps="true"/>
    </LinearLayout>

</RelativeLayout>
